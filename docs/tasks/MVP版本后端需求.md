我需要完成一个网络小说作家作品管理系统，其相关功能在 "user-storys.md" 中已经写明

当前任务：
1、当前计划结合已经完成MVP版本前端部分的功能完成后端功能需求，并且也要考虑清楚后续版本的良好扩展性。
2、后端架构上需要参考cdk的后端架构来实现吗，因此你需要读取cdk目录的代码，并生成详细的技术需求文档。
3、我们当前已经执行了"steps"路径下的所有step来完成前端部分功能，你需要对照用户需求并仿照前端完成的过程来完成后端部分的功能，并确保每一个功能步骤都被写入到step文件中。
4、如果你要委派执行**子任务**，则在**子任务**的**TODO list**的最后一项中需要增加一个**参考steps模板追加对应step文件**的待办项，在这一项里，你被授权在`steps`文件夹内的对应目录下追加`step文件`，在`step文件`中，`steps/templates/answer-template.md`是需要参考的**steps模板**，你必须严格按照**steps模板**的格式将**子任务**的执行过程记录到追加的`step文件`中，`step文件`的文件名要符合已有文件的命名规律。
5、当需要修改已有的代码时，你需要先读取当前代码，然后再给出需要修改的内容建议，不可以凭空想象代码。