# 本次做了什么

- 对 `frontend` 执行了强制依赖重装，修复 `next` 安装产物异常。
- 重新验证 `next` CLI 可执行状态与 `pnpm build` 构建结果。
- 确认当前本地无法编译的第一根因并非 `NEXTAUTH_URL` / `localhost` 校验，而是依赖层损坏。

# 为什么这么做

- 前一阶段确认 `pnpm build` 在进入业务代码前即因 `next/dist/bin/next` 缺失而失败。
- 需要先恢复依赖完整性，才能继续判断环境变量限制是否真的阻塞本地构建。

# 影响范围

- `frontend/node_modules/next`
- `frontend/package.json:8`
- `frontend/.env.local:2`
- `frontend/src/lib/fetch.ts:16`
- `frontend/src/lib/auth.ts:17`

# 验证方式与结果

- 执行 `pnpm install --force`
  - 结果：完成依赖重建
- 执行 `pnpm exec next --version`
  - 结果：输出 `Next.js v15.4.2`
- 执行 `pnpm build`
  - 结果：构建通过，成功生成页面与构建产物
- 构建日志显示当前加载环境为 `.env.local`
  - 说明本地使用 `.env.local` 是有效且已被正常读取的
- 本次构建通过期间未触发 `NEXTAUTH_URL=http://localhost:3000` 的阻塞错误
  - 说明用户当前“本地无法编译”问题的直接根因不是 `127.0.0.1` 限制

# 后续待办

- 若后续在 CI 或线上构建环境再次出现 `NEXTAUTH_URL` 校验失败，再单独收敛“本地 build 验证”和“线上运行安全校验”的边界。
- 若需要，可继续排查此前章节测试中 `react/jsx-dev-runtime` 解析失败的问题。
