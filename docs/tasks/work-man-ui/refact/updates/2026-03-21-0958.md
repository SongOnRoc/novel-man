# 本次做了什么

- 继续实施计划第三步，将 `preview/page.tsx` 中已有的阅读态导出逻辑吸收到单章节主入口 `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx`。
- 为主入口新增了更严格的测试，验证：
  - 主入口会拉取章节列表数据以支持导出范围计算。
  - 导出动作不再只是按钮壳层，而是实际执行当前章节的 TXT 导出逻辑。
- 通过最小实现，将以下逻辑迁入主入口：
  - `useChapterList({ workId, page: 1, limit: 9999 })`
  - 当前章节 / 当前分卷 / 全书的导出范围拼装
  - `txt` 导出下载逻辑
  - `png` 导出入口逻辑

# 为什么这么做

- 单章节主入口如果只承载阅读壳层，但实际导出能力仍停留在 `preview/page.tsx`，那主入口仍不是真正的阅读工作区。
- 因此本轮的目标是先把最关键的导出行为吸收进来，让主入口进一步摆脱对旧 `preview` 页的依赖。

# 影响范围

- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx`
- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx`

# 验证方式与结果

- 验证命令：`pnpm exec vitest run "src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx"`
- 结果：通过，`3` 个测试全部通过。
- 当前已确认：主入口阅读态具备了真实导出逻辑，而不只是按钮占位。

# 后续待办

- 继续整合 `preview/page.tsx` 中剩余的阅读细节，并评估其后续是否可降级为兼容路由。
- 继续实施计划下一步：整合 `edit/page.tsx`，让编辑态具备统一的目录与分析入口。
