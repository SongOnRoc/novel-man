# 本次做了什么

- 根据用户对上一版实现的直接反馈，重新审视了 `frontend/src/app/(main)/works/[id]/page.tsx` 的顶部布局。
- 确认上一版的主要问题是虽然移除了外层大卡，但仍保留了“4 个统计项分别占据独立模块面积”的错误思路。
- 将顶部区域进一步改为单列信息流：作品信息、操作按钮、统计指标都回到同一内容流中，不再形成左右分栏与独立统计卡片。
- 同步收紧了 loading skeleton，避免加载态继续暗示厚重大卡片结构。

# 为什么这么做

- 用户的反馈明确指出：核心问题不是信息多少，而是 UI 把很少的信息做成了占屏很大的模块。
- 继续保留独立统计块会让移动端和桌面端都显得松散、空洞、低效，因此必须彻底撤掉“统计卡”思路，改为更接近工作台的内联指标展示。

# 影响范围

- 页面实现：`frontend/src/app/(main)/works/[id]/page.tsx`

# 验证方式与结果

- 执行：`pnpm exec eslint "src/app/(main)/works/[id]/page.tsx"`
- 结果：通过

# 后续待办

- 等待用户再次查看 `/works/47` 当前效果，确认顶部密度与整体气质是否已经接近“高效、优雅、现代”的方向。
- 若仍有局部视觉问题，继续对 tabs 区和列表区做第二轮压缩与层级调整。
