# 本次做了什么

- 继续整合章节编辑态，使 `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.tsx` 接入统一章节工作区头部。
- 新增编辑态测试：
  - `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.test.tsx`
- 通过最小改动让编辑页具备：
  - 统一的章节工作区页头
  - 目录入口
  - 分析入口占位
  - 返回阅读态入口
  - 保留现有 `TiptapEditor` 作为正文编辑核心

# 为什么这么做

- 编辑页如果继续是完全独立的孤立页面，后续“阅读 / 编辑 / 分析”就无法在同一套章节工作区语义下收敛。
- 因此本轮优先在编辑页外层接入统一头部，而不是急着改编辑器内部复杂逻辑。

# 影响范围

- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.tsx`
- 新增：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.test.tsx`

# 验证方式与结果

- 验证命令：`pnpm exec vitest run "src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.test.tsx"`
- 结果：通过，`1` 个测试全部通过。
- 说明：本轮只完成了编辑页与统一章节工作区头部的接轨，分析能力仍是入口占位，尚未接入真实工作区容器。

# 后续待办

- 下一步继续实现章节分析工作区容器，让阅读态和编辑态都能打开统一的分析层。
- 后续再进入段落选择模式，实现从正文到分析的完整链路。
