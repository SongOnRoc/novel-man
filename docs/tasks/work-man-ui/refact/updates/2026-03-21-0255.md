# 本次做了什么

- 按实施计划第二步，将 `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx` 从纯重定向页升级为单章节工作区主入口。
- 新增了两个最小基础组件：
  - `frontend/src/features/chapters/components/ChapterWorkspaceHeader.tsx`
  - `frontend/src/features/chapters/components/ChapterReader.tsx`
- 新增单章节主入口测试：
  - `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx`
- 当前单章节主入口已经具备：
  - 章节工作区页头
  - 目录入口
  - 分析入口占位
  - 编辑入口
  - 导出入口
  - 阅读态正文渲染

# 为什么这么做

- 章节系统如果继续让 `[chapterId]/page.tsx` 只做跳转，后续目录、阅读、编辑、分析和片段选择的统一任务流就无法建立。
- 因此本轮先做最小工作区壳层，把默认章节入口从“跳往 preview”改为“直接进入章节工作区”。

# 影响范围

- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx`
- 新增：`frontend/src/features/chapters/components/ChapterWorkspaceHeader.tsx`
- 新增：`frontend/src/features/chapters/components/ChapterReader.tsx`
- 新增：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx`

# 验证方式与结果

- 验证命令：`pnpm exec vitest run "src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx"`
- 结果：通过，`2` 个测试全部通过。
- 说明：本轮仅验证单章节主入口壳层本身，不代表预览页逻辑已经完全迁移完成；导出入口目前先保留为工作区中的壳层动作。

# 后续待办

- 继续实施计划第三步：把 `preview/page.tsx` 的阅读与导出逻辑进一步吸收到单章节页阅读态中。
- 再继续整合 `edit/page.tsx`，使编辑态具备统一的目录与分析入口。
