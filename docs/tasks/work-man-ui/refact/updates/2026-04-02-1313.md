# 本次做了什么

- 删除了移动端线框的历史残留导出图：`docs/tasks/work-man-ui/refact/design/VWNM8.png`。
- 统一了移动端“展开态”和“Header 收缩态”两张线框图的底部 sticky 按钮布局：
  - `更多` 固定在右侧。
  - `新建草稿` 不再占满左侧空间，改为 `fit_content` 的轻量按钮。
- 追加了“更多打开态”的线框预览：点击底部 `更多` 后展开浮层，菜单项与客户端保持一致（新建章节、编辑作品）。

# 为什么这么做

- `VWNM8.png` 为早期探索稿，已不再被设计说明引用，保留只会造成困惑。
- 用户明确要求：`更多` 在右侧，但不意味着左侧按钮应占满剩余空间；`新建草稿` 需要更轻量。
- 同一交互在不同状态（展开/收缩）按钮布局不一致，会增加学习成本与割裂感，需要统一。

# 影响范围

- 删除文件：`docs/tasks/work-man-ui/refact/design/VWNM8.png`
- 更新导出：
  - `docs/tasks/work-man-ui/refact/design/dBpvw.png`（移动端展开态）
  - `docs/tasks/work-man-ui/refact/design/G5YEB.png`（移动端更多打开态）

注：更多打开态已单独导出，不再覆盖默认态。

# 验证方式与结果

- pencil 截图核对：移动端 sticky 底部栏中 `更多` 固定右侧，且 `新建草稿` 视觉占比明显降低。

# 后续待办

- 定义“更多”的 ActionSheet 打开态（列表项、分组、危险操作、关闭方式），并生成线框预览。
