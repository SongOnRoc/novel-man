# 本次做了什么

- 将阅读态已经具备的“分析 + 片段选择”交互继续接入编辑态。
- 修改 `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.tsx`，使编辑页外层支持：
  - 打开分析面板
  - 进入片段选择模式
  - 从片段选择模式回流到分析面板
- 调整 `ChapterWorkspaceHeader`，使“选择片段提取”按钮不再只限于阅读态，而是由是否提供 `onStartSelection` 决定是否显示。
- 扩展编辑态测试，验证：
  - 编辑态显示片段选择入口
  - 点击分析后可打开桌面端分析面板容器

# 为什么这么做

- 如果阅读态和编辑态的分析/选择入口语义不同，章节工作区就仍然是割裂的。
- 当前先在编辑页外层复用已有工作区组件与状态流，尽量不改 `TiptapEditor` 内核，从而降低改动风险。

# 影响范围

- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.tsx`
- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.test.tsx`
- 修改：`frontend/src/features/chapters/components/ChapterWorkspaceHeader.tsx`

# 验证方式与结果

- 验证命令：`pnpm exec vitest run "src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.test.tsx"`
- 结果：通过，`2` 个测试全部通过。
- 当前确认：编辑态已经与阅读态共享同一套分析入口和片段选择入口语义。

# 后续待办

- 如果继续推进，应开始把分析工作区从当前壳层升级为真实的大纲/角色提取结果工作区。
- 另外还需要回看整体编译链，把当前尚未处理完的构建阻塞继续清理，避免前端处于“局部测试通过但整体 build 失败”的状态。
