# 本次做了什么

- 开始为单章节阅读态接入真实的分析工作区容器。
- 新增两个基础组件：
  - `frontend/src/features/chapters/components/ChapterAnalysisPanel.tsx`
  - `frontend/src/features/chapters/components/ChapterAnalysisSheet.tsx`
- 扩展了 `ChapterWorkspaceHeader`，支持通过 `onOpenAnalysis` 打开分析工作区。
- 在 `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx` 中接入：
  - 桌面端分析面板
  - 移动端分析 sheet
  - 分析开关状态
- 为阅读态补充测试，确认桌面端点击“分析”后会打开侧边分析面板。

# 为什么这么做

- 目录页、单章节主入口和编辑态头部都已经接起来了，下一步需要让“分析”从占位入口变成真实工作区容器。
- 当前先做容器壳层，可以在不提前引入复杂提取逻辑的前提下，先把页面职责和承载方式搭对。

# 影响范围

- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx`
- 修改：`frontend/src/features/chapters/components/ChapterWorkspaceHeader.tsx`
- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx`
- 新增：`frontend/src/features/chapters/components/ChapterAnalysisPanel.tsx`
- 新增：`frontend/src/features/chapters/components/ChapterAnalysisSheet.tsx`

# 验证方式与结果

- 验证命令：`pnpm exec vitest run "src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx"`
- 结果：通过，`4` 个测试全部通过。
- 当前确认：桌面端阅读态已能打开真实分析面板容器，移动端容器壳层也已接入，但尚未补移动端专门测试。

# 后续待办

- 继续把分析工作区接入编辑态，让阅读与编辑共用同一套分析入口语义。
- 再进入下一阶段：设计并接入段落选择模式，实现从正文到分析的碎片化提取链路。
