# 本次做了什么

- 完成了章节系统最后一块核心交互的最小壳层：段落选择模式。
- 新增组件：
  - `frontend/src/features/chapters/components/ChapterSelectionMode.tsx`
  - `frontend/src/features/chapters/components/SelectionActionBar.tsx`
- 扩展了以下组件以支持选择模式和分析输入标签：
  - `frontend/src/features/chapters/components/ChapterWorkspaceHeader.tsx`
  - `frontend/src/features/chapters/components/ChapterAnalysisPanel.tsx`
  - `frontend/src/features/chapters/components/ChapterAnalysisSheet.tsx`
  - `frontend/src/features/chapters/components/ChapterReader.tsx`
- 在 `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx` 中接入：
  - “选择片段提取”入口
  - 片段选择模式
  - 选择后将结果送入分析工作区

# 为什么这么做

- 前面的目录页、单章节主入口、分析工作区都已经有了壳层，但没有“从正文中选择输入范围”的链路，碎片化提取还不成立。
- 本轮先做最小可运行的选择模式，把整个“选择 -> 提取 -> 进入分析”的闭环搭起来。

# 影响范围

- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx`
- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx`
- 修改：`frontend/src/features/chapters/components/ChapterWorkspaceHeader.tsx`
- 修改：`frontend/src/features/chapters/components/ChapterAnalysisPanel.tsx`
- 修改：`frontend/src/features/chapters/components/ChapterAnalysisSheet.tsx`
- 修改：`frontend/src/features/chapters/components/ChapterReader.tsx`
- 新增：`frontend/src/features/chapters/components/ChapterSelectionMode.tsx`
- 新增：`frontend/src/features/chapters/components/SelectionActionBar.tsx`

# 验证方式与结果

- 验证命令：`pnpm exec vitest run "src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx"`
- 结果：通过，`5` 个测试全部通过。
- 当前确认：阅读态可以进入片段选择模式，选中段落后可进入分析工作区，并显示“已选 N 段”的输入对象语义。

# 后续待办

- 继续把同一套分析入口和选择模式语义接入编辑态，而不仅仅是阅读态。
- 后续如果进入下一阶段，需要开始收敛真实的大纲/角色提取结果与分析状态管理，而不是继续停留在容器壳层。
