# 本次做了什么

- 继续把分析工作区从单一“大纲结果”推进到真正具备“角色结果”分支。
- 扩展阅读态测试，新增场景：
  - 进入片段选择模式后点击“提取角色经历”
  - 应生成角色结果而不是继续走大纲结果
- 修正阅读态与编辑态中的结果生成逻辑，使其真正使用 `onExtract` 返回的 `type` 字段。

# 为什么这么做

- 如果“提取角色经历”仍然只展示大纲结果，分析工作区虽然有结果态，但业务语义还是假的。
- 本轮目标是让“提取大纲”和“提取角色经历”至少在最小实现层面分成两条真实分支。

# 影响范围

- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx`
- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.tsx`
- 修改：`frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx`

# 验证方式与结果

- 验证命令：
  - `pnpm exec vitest run "src/app/(main)/works/[id]/chapters/[chapterId]/page.test.tsx"`
  - `pnpm exec vitest run "src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.test.tsx"`
- 结果：全部通过。
- 当前确认：
  - 片段选择后点击“提取大纲”会进入大纲结果态。
  - 片段选择后点击“提取角色经历”会进入角色结果态。
  - 编辑态回归测试也保持通过。

# 后续待办

- 如果继续推进，可进一步把“整章分析”从默认固定走大纲结果扩展为可切换分析类型。
- 也可以回到工程收口，重新检查整体构建与类型问题，把当前实现从“局部测试通过”推进到“整体可构建”。
