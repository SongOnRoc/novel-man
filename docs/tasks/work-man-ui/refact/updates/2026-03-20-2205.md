# 本次做了什么

- 按照实施计划的第一步，开始把 `chapters/page.tsx` 从“章节管理页”重构为“章节目录页”骨架。
- 先通过测试收紧了页面目标：
  - 目录工具条必须出现
  - 洞察卡片不应继续出现
  - 低频动作应收敛到“更多操作”语义
- 新增了两个基础组件：
  - `frontend/src/features/chapters/components/ChapterDirectoryToolbar.tsx`
  - `frontend/src/features/chapters/components/ChapterTocTree.tsx`
- 将 `frontend/src/app/(main)/works/[id]/chapters/page.tsx` 调整为新的目录页骨架，并保留现有删除、导入、重排对话框逻辑。

# 为什么这么做

- 章节系统重构的第一步不是加分析能力，而是先把页面身份改对。
- 如果目录页继续保留旧的后台管理结构，后续再加分卷、目录折叠、分析入口只会继续变乱。
- 因此本轮优先完成页面骨架重构，并通过测试锁定新方向。

# 影响范围

- 修改：`frontend/src/app/(main)/works/[id]/chapters/page.tsx`
- 修改：`frontend/src/app/(main)/works/[id]/chapters/page.test.tsx`
- 新增：`frontend/src/features/chapters/components/ChapterDirectoryToolbar.tsx`
- 新增：`frontend/src/features/chapters/components/ChapterTocTree.tsx`

# 验证方式与结果

- 验证命令：`pnpm exec vitest run "src/app/(main)/works/[id]/chapters/page.test.tsx"`
- 结果：通过，`8` 个测试全部通过。
- 说明：项目里存在与本轮任务无关的 `EditorToolbar` 测试失败噪音，因此本轮验证采用精确单文件测试命令，确认章节目录页改动本身已通过。

# 后续待办

- 继续进入实施计划第二步：将 `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx` 从重定向页升级为单章节主入口。
- 目录页后续还需要继续增强为真正的卷/章树结构，目前只是先完成了新骨架与基础目录组件。
