# 本次做了什么

- 读取 `frontend/src/app/(main)/works/[id]/page.tsx`，重新梳理作品详情页的现有结构与信息分布。
- 根据当前会话结论，确认本页的核心问题是 UI 层级失衡、卡片过多、移动端单项信息占屏过大，而不是数据量本身。
- 形成了本页的重构设计与执行计划，并写入任务文档，作为后续逐页面优化的统一基线。

# 为什么这么做

- 当前页面的真实问题在于视觉结构和响应式设计，而不是缺少内容或逻辑。
- 在继续逐页面优化前，需要先把“信息不减、交互不减、只重构 UI”的原则固定下来，避免后续实现偏成删内容或改业务。

# 影响范围

- 页面实现：`frontend/src/app/(main)/works/[id]/page.tsx`
- 设计文档：`docs/tasks/work-man-ui/refact/design/2026-03-30-1913.md`
- 计划文档：`docs/tasks/work-man-ui/refact/plan/2026-03-30-1913.md`

# 验证方式与结果

- 本阶段为设计与范围确认阶段，暂未执行代码验证命令。
- 下一阶段会在完成页面改造后执行至少一轮定向验证，确认页面改动未引入语法或构建层面的明显问题。

# 后续待办

- 按本轮确认的跨端 UI 方向重构作品详情页顶部信息区与管理区。
- 压缩章节/草稿列表项密度，并同步更新 skeleton。
- 完成当前页面后，再与用户确认下一页的优化目标。
