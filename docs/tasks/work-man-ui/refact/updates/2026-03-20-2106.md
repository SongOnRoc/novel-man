# 本次做了什么

- 对照现有章节相关路由与组件，完成了一轮“新设计 -> 现有代码结构”的映射分析。
- 阅读并比对了以下文件：
  - `frontend/src/app/(main)/works/[id]/chapters/page.tsx`
  - `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx`
  - `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/preview/page.tsx`
  - `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.tsx`
  - `frontend/src/app/(main)/works/[id]/chapters/components/TOCChapterList.tsx`
  - `frontend/src/app/(main)/works/[id]/chapters/components/chapter-list.tsx`
- 将映射结论写入 `design/2026-03-20-2106.md`。

# 为什么这么做

- 统一设计方案已经明确了章节系统的新职责拆分，下一步必须确认现有代码里哪些资产可保留、哪些页面要改角色，否则无法进入实施方案阶段。
- 这一步的目标是减少后续实现时的反复摇摆。

# 影响范围

- 新增设计文档：`docs/tasks/work-man-ui/refact/design/2026-03-20-2106.md`
- 新增阶段更新：`docs/tasks/work-man-ui/refact/updates/2026-03-20-2106.md`
- 关键判断涉及页面角色调整：
  - `frontend/src/app/(main)/works/[id]/chapters/page.tsx`
  - `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/page.tsx`
  - `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/preview/page.tsx`
  - `frontend/src/app/(main)/works/[id]/chapters/[chapterId]/edit/page.tsx`

# 验证方式与结果

- 验证方式：静态阅读章节相关页面和组件，对照统一设计方案逐一判断当前页面角色与未来页面角色是否一致。
- 结果：
  - `chapters/page.tsx` 应保留路由但重写页面身份。
  - `[chapterId]/page.tsx` 不应继续只是重定向页，应升级为单章节主入口。
  - `preview/page.tsx` 不应作为未来核心承载页，优先并入单章节阅读态。
  - `edit/page.tsx` 可保留其编辑器基础，但应纳入统一章节工作区结构。

# 后续待办

- 继续把这份结构映射收敛成真正的实施方案。
- 明确下一阶段先改目录页还是先改单章节主入口。
- 如果进入实现前规划，还需要进一步定义组件拆分边界和迁移顺序。
