# 本次做了什么

- 检查了当前工作区中的前端相关 diff，并聚焦草稿发布流程、章节管理页、章节号调整能力和相关测试。
- 阅读了以下关键文件：
  - `frontend/src/app/(main)/drafts/page.tsx`
  - `frontend/src/app/(main)/drafts/[id]/edit/page.tsx`
  - `frontend/src/app/(main)/works/[id]/chapters/page.tsx`
  - `frontend/src/app/(main)/works/[id]/chapters/components/TOCChapterList.tsx`
  - `frontend/src/features/drafts/components/PublishDraftDialog.tsx`
  - `frontend/src/features/chapters/lib/chapterNumbering.ts`
  - `frontend/src/features/chapters/lib/chapterReorder.ts`
- 输出了本次前端代码审查报告，并将结论写入 `review/2026-03-20-2023.md`。
- 新建了本任务的 `proposal.md`，将本次 review 的背景、目标、范围和约束补齐。

# 为什么这么做

- 当前前端改动跨度较大，且草稿发布与章节重排都属于用户高频核心链路，提交前需要优先排查行为级风险。
- 仓库规则要求将阶段性结论落盘到 `docs/tasks/`，避免审查结果只存在于会话上下文中，便于后续修复和复核。

# 影响范围

- 文档新增：`docs/tasks/work-man-ui/refact/proposal.md`
- 审查记录：`docs/tasks/work-man-ui/refact/review/2026-03-20-2023.md`
- 阶段更新：`docs/tasks/work-man-ui/refact/updates/2026-03-20-2023.md`
- 重点审查的业务代码位置：
  - `frontend/src/app/(main)/drafts/page.tsx:316`
  - `frontend/src/app/(main)/drafts/page.tsx:455`
  - `frontend/src/app/(main)/drafts/[id]/edit/page.tsx:195`
  - `frontend/src/app/(main)/works/[id]/chapters/page.tsx:97`
  - `frontend/src/app/(main)/works/[id]/chapters/page.tsx:214`

# 验证方式与结果

- 验证方式：基于 `git status`、`git diff`、文件阅读、测试文件阅读进行静态代码审查。
- 结果：已识别出 3 个需优先修复的问题和 2 个非阻塞改进项。
- 未执行测试或构建；原因：本次用户请求是 review 当前改动，目标是输出代码审查结论，而不是验证通过性。
- 若后续需要验证，建议命令：
  - `pnpm test -- --run "frontend/src/app/(main)/drafts/page.test.tsx"`
  - `pnpm test -- --run "frontend/src/app/(main)/works/[id]/chapters/page.test.tsx"`

# 后续待办

- 修复草稿发布流程中的 `selectedWorkId` 残留与 `Select` 非受控问题。
- 评估并改进章节重排的一致性方案，优先考虑批量原子接口。
- 补充发布弹窗、连续发布和分页边界场景测试。
- 视需要继续补写 `evaluation/` 或新的 `review/` 文档，记录修复后的复审结果。
