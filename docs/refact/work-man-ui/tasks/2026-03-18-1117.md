# 本次做了什么

- 为章节重排补充“空操作”页面测试：当目标章节号与当前章节号一致时，不应发送任何 `updateChapter` 请求，也不应弹成功/失败提示。

# 为什么这么做

- 这是重排行为的一个典型边界条件。若不测试，后续 UI 调整时容易把无效操作变成真实写入，造成不必要请求与提示噪音。

# 影响范围

- `frontend/src/app/(main)/works/[id]/chapters/page.test.tsx`

# 验证方式与结果

- 运行：`pnpm -C frontend test --run "src/app/(main)/works/[id]/chapters/page.test.tsx"`
- 结果：通过。

# 后续待办

- 给重排对话框增加即时预览文案（如“将变为第X章”）。
- 视需要补充“越界章节号输入”的页面交互测试。
