# 本次做了什么

- 按确认结果，将全局草稿箱 `/drafts` 的发布成功行为改为跳转到对应作品章节页 `/works/{workId}/chapters`。

# 为什么这么做

- 作品内草稿发布成功后已经会回到章节列表；全局草稿箱也统一跳转后，用户能立即看到新发布章节，入口体验一致。

# 影响范围

- `frontend/src/app/(main)/drafts/page.tsx`

# 验证方式与结果

- 运行单测：`pnpm -C frontend test --run "src/app/(main)/drafts/page.test.tsx"`
- 结果：通过。

# 后续待办

- 为 `/drafts` 的“选择作品 -> 发布 -> 跳转”链路补一个更完整的行为测试。
- 继续推进章节编号样式设置 UI 与章节重排能力。
