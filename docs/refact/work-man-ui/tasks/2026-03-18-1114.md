# 本次做了什么

- 为章节重排补齐失败分支页面测试：当逐条 `updateChapter` 过程中有任一请求失败时，页面应 toast 报错并触发 `router.refresh()`。

# 为什么这么做

- proposal 已明确重排阶段存在“部分成功、部分失败”的风险，前端必须在失败后回到服务端真实状态。
- 这类恢复逻辑很容易在后续重构中被遗漏，因此需要页面级测试兜底。

# 影响范围

- `frontend/src/app/(main)/works/[id]/chapters/page.test.tsx`

# 验证方式与结果

- 运行：`pnpm -C frontend test --run "src/app/(main)/works/[id]/chapters/page.test.tsx"`
- 结果：通过。

# 后续待办

- 给重排对话框增加即时预览文案（如“将变为第X章”），提升用户心智清晰度。
- 视需要再补一条“目标章节号等于当前号时不提交请求”的页面测试。
