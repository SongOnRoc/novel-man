# 本次做了什么

- 审查了当前分支与作品工作台相关的前端改动，重点查看了全局草稿发布链路、作品章节重排链路、outline/world 页面结构，以及对应的测试覆盖。
- 对照 `docs/refact/work-man-ui/proposal.md` 检查实现是否符合本次任务定义与验收标准。
- 形成了一份代码评审报告，落盘到 `docs/refact/work-man-ui/review/2026-03-19.md`。

# 为什么这么做

- 这批改动同时涉及路由职责、数据更新顺序、章节编号规则和 UI 壳层一致性，属于容易引入回归的前端重构区域。
- 在合入前先把高风险问题和测试缺口明确写清，可以避免将“方向正确但链路不稳”的改动直接带入主分支。

# 影响范围

- `frontend/src/app/(main)/drafts/page.tsx`
- `frontend/src/app/(main)/drafts/[id]/edit/page.tsx`
- `frontend/src/features/drafts/components/PublishDraftDialog.tsx`
- `frontend/src/app/(main)/works/[id]/chapters/page.tsx`
- `frontend/src/app/(main)/works/[id]/chapters/components/TOCChapterList.tsx`
- `frontend/src/features/chapters/lib/chapterReorder.ts`
- `frontend/src/app/(main)/works/[id]/outline/page.tsx`
- `frontend/src/app/(main)/works/[id]/world/page.tsx`
- `frontend/src/app/(main)/drafts/page.test.tsx`
- `frontend/src/app/(main)/works/[id]/chapters/page.test.tsx`

# 验证方式与结果

- 验证方式：阅读当前 git diff、相关页面/组件/测试文件内容，并结合任务 proposal 对照审查。
- 结果：识别出 2 个需要优先处理的问题：
  - 全局草稿发布时存在 `selectedWorkId` 沿用上一次选择、导致误发布到错误作品的风险。
  - outline/world 当前实现没有按 proposal 接入工作台壳层，与验收标准不一致。
- 未执行自动化测试；下一步建议命令：
  - `pnpm --dir frontend test -- --run "src/app/(main)/drafts/page.test.tsx" "src/app/(main)/works/[id]/chapters/page.test.tsx"`
  - `pnpm --dir frontend lint`
  - `pnpm --dir frontend build`

# 后续待办

- 修复全局草稿发布状态串单问题，并补上对应回归测试。
- 明确 outline/world 到底是继续使用独立布局，还是按 proposal 接入 `WorkWorkspaceLayout`；无论选择哪种，都要同步代码和任务文档。
- 评估章节重排逐条更新的失败恢复策略，至少补足测试和用户提示。
