# 本次做了什么

- 进入执行阶段，补齐任务门禁文档：新增 `docs/refact/work-man-ui/proposal.md`。
- 生成当日执行计划：新增 `docs/refact/work-man-ui/plan/2026-03-16.md`，将“先前端分阶段执行”的重点固化为可验收步骤。

# 为什么这么做

- 按仓库规则，开始任何实质执行前必须存在 proposal；同时需要把前端优先、display_order 权威、发布强制修正、编号样式（全局+作品覆盖）等关键决策写入可追溯文档，避免后续跑偏。

# 影响范围

- 仅新增过程文档，不改动业务逻辑与 UI 代码。

# 验证方式与结果

- 验证方式：检查新增文档路径是否存在且结构完整。
- 结果：
  - `docs/refact/work-man-ui/proposal.md` 已创建并包含背景/目标/范围/验收/风险/计划。
  - `docs/refact/work-man-ui/plan/2026-03-16.md` 已创建并按阶段拆解前端执行步骤。

# 后续待办

- 前端阶段 0：实现编号样式存储（全局默认+作品覆盖）与标题归一化工具（含中文数字 1-9999）。
- 前端阶段 1：outline/world 接入 `WorkWorkspaceLayout + WorkModuleSection`。
