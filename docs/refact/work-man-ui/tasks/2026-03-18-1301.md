# 本次做了什么

- 按最新范围要求，将作品子页面改回各自独立样式：不再复用作品主页的上下壳层结构。
- 保留 `/works/[id]` 主页的上下结构不动，仅调整子页面：
  - `chapters`
  - `drafts`
  - `characters`
  - `outline`
  - `world`
- 同步更新了相关页面测试，使断言与“子页面独立样式”一致。

# 为什么这么做

- 当前任务边界被重新明确：只有作品主页需要上下结构，子页面必须保留各自独立的视觉与信息组织方式。
- 之前把子页面统一套壳层，破坏了各页面的独立表达和交互氛围，不符合产品要求。

# 影响范围

- `frontend/src/app/(main)/works/[id]/chapters/page.tsx`
- `frontend/src/app/(main)/works/[id]/drafts/page.tsx`
- `frontend/src/app/(main)/works/[id]/characters/page.tsx`
- `frontend/src/app/(main)/works/[id]/outline/page.tsx`
- `frontend/src/app/(main)/works/[id]/world/page.tsx`
- `frontend/src/app/(main)/works/[id]/chapters/page.test.tsx`
- `frontend/src/app/(main)/works/[id]/drafts/page.test.tsx`
- `frontend/src/app/(main)/works/[id]/characters/page.test.tsx`

# 验证方式与结果

- 运行：`pnpm -C frontend test --run "src/app/(main)/works/[id]/chapters/page.test.tsx" "src/app/(main)/works/[id]/drafts/page.test.tsx" "src/app/(main)/works/[id]/characters/page.test.tsx"`
- 结果：通过。

# 后续待办

- 如果还需要进一步“改回原来的味道”，下一步应该逐页对照你认可的旧视觉细节，而不是再抽象统一壳层。
- 对 `outline/world` 当前仍是原型内容，可后续单独逐页收拾，但不能再套作品主页结构。
