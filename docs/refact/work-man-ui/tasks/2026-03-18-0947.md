# 本次做了什么

- 校验 `docs/refact/work-man-ui/tasks/2026-03-18-0940.md` 中列出的“影响范围”文件路径是否真实存在，并抽查文件内容与描述是否一致。
- 校验“草稿发布弹框”是否满足 proposal 的强约束：提示将发布为第X章、章号冲突强制修正、publish 前先更新草稿标题。
- 补强发布弹框的冲突覆盖面：默认输入从草稿标题剥离前缀后作为“后缀输入”，同时仍对“原草稿标题是否包含冲突章号”做检测并展示修正前/后，避免用户通过“仅编辑后缀”绕过冲突提示。

# 为什么这么做

- 任务文档必须可追溯且与代码一致；如果文档引用了不存在的路径或描述与实现不符，会导致后续推进（发布/重排/样式设置入口）建立在错误假设上。
- 发布链路对标题的约束是本任务验收项之一，且后端使用 draft.title 生成 chapter.title，必须确保前端在 publish 前完成归一化与冲突强制修正。

# 影响范围

- 文档校验对象：`docs/refact/work-man-ui/tasks/2026-03-18-0940.md`
- 关键代码路径（与文档一致）：
  - `frontend/src/features/chapters/lib/chapterNumbering.ts`
  - `frontend/src/features/chapters/lib/chapterNumberingStorage.ts`
  - `frontend/src/features/drafts/components/PublishDraftDialog.tsx`
  - `frontend/src/app/(main)/works/[id]/drafts/[draftId]/edit/page.tsx`
  - `frontend/src/app/(main)/works/[id]/drafts/[draftId]/edit/page.test.tsx`

# 验证方式与结果

- 路径与内容一致性：通过工具查找/读取上述文件确认存在且行为匹配文档描述。
- 单测（抽样覆盖本次新增与接入点）：
  - `pnpm -C frontend test --run src/features/chapters/lib/chapterNumbering.test.ts src/features/chapters/lib/chapterNumberingStorage.test.ts "src/app/(main)/works/[id]/drafts/[draftId]/edit/page.test.tsx"`
  - 结果：通过

# 后续待办

- 将同样的发布弹框能力接入 `/works/[id]/drafts` 列表页的“发布为章节”入口（目前列表直接 publish）。
- 将同样的发布弹框能力接入全局草稿编辑页 `/drafts/[id]/edit`（需先明确/选择 workId 再计算 chapterNo）。
