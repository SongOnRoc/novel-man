# 本次做了什么

- 为章节重排对话框增加即时预览文案：当输入目标章节号后，会显示“将变为：第X章”。
- 补充对应页面测试，确保打开对话框时就能看到基于当前章节号生成的预览文案。

# 为什么这么做

- 在不改变业务逻辑的前提下，预览文案能帮助用户更直观地理解本次调整结果，减少误操作。
- 该预览复用了当前编号样式配置，因此后续接入编号样式设置 UI 时也能自然联动。

# 影响范围

- `frontend/src/app/(main)/works/[id]/chapters/page.tsx`
- `frontend/src/app/(main)/works/[id]/chapters/page.test.tsx`

# 验证方式与结果

- 运行：`pnpm -C frontend test --run "src/app/(main)/works/[id]/chapters/page.test.tsx"`
- 结果：通过。

# 后续待办

- 可继续补“越界章节号输入”的页面测试与提示文案。
- 若后续加入编号样式设置 UI，可让此预览实时响应样式切换。
