# 本次做了什么

- 为章节重排补充页面级行为测试，覆盖“打开调整章节号 -> 修改目标号 -> 确认调整 -> 逐条提交更新 payload”的完整链路。
- 新增断言：向前插入时，只对受影响章节调用 `mutateAsync`，并按顺序提交修正后的 `displayOrder/title`。

# 为什么这么做

- 之前已有纯逻辑 `chapterReorder` 单测，但页面层还缺少“最终是否按预期发出 update payload”的保护。
- 这一层测试能更直接地防止未来改 UI/弹框状态时把重排提交逻辑弄坏。

# 影响范围

- `frontend/src/app/(main)/works/[id]/chapters/page.test.tsx`

# 验证方式与结果

- 运行：`pnpm -C frontend test --run "src/app/(main)/works/[id]/chapters/page.test.tsx"`
- 结果：通过。

# 后续待办

- 补一个失败分支测试：当某条 `updateChapter` 失败时，应 toast 报错并触发刷新。
- 视体验需要，补充对话框中的即时预览文案（例如“将变为第X章”）。
