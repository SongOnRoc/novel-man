# 本次做了什么

- 核对 `/works/[id]` 主页与各作品子页面的边界，确认现在只有作品主页保留上下结构。
- 核对结果：
  - `/works/[id]` 主页仍保留作品管理上下布局。
  - `/works/[id]/chapters`、`/works/[id]/drafts`、`/works/[id]/characters`、`/works/[id]/outline`、`/works/[id]/world` 已改回各自独立样式。
  - 子页面不再使用 `WorkWorkspaceLayout` / `WorkModuleSection` 这套主页式壳层。

# 为什么这么做

- 这一步是为了把需求边界收干净：主页和子页面的布局职责必须明确分离，避免后续继续串改。

# 影响范围

- 本次主要是边界核对与验证，无新增功能改动。

# 验证方式与结果

- 搜索核对：仅 `frontend/src/app/(main)/works/[id]/page.tsx` 仍保留主页级内容管理结构；子页面主要改为 `PageHeader + 各自内容区`。
- 运行：`pnpm -C frontend test --run "src/app/(main)/works/[id]/page.test.tsx" "src/app/(main)/works/[id]/chapters/page.test.tsx" "src/app/(main)/works/[id]/drafts/page.test.tsx" "src/app/(main)/works/[id]/characters/page.test.tsx"`
- 结果：通过。

# 后续待办

- 如果还要继续收敛体验，应该按“逐个子页面独立优化”的方式做，而不是再抽一层统一作品壳层。
