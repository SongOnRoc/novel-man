# 作品管理
http://localhost:3000/works/7
1、作品管理页面分为 上下布局结构，上半部分是作品基本信息，下半部分多标签是作品的内容管理，标签分别是章节列表，草稿列表（已绑定该作品），作品大纲，作品角色，作品设定（世界观设定）等
2、上半部分区域又分为左右布局结构，左边是作品封面，右边是作品基本信息
3、上述描述均需要同时考虑移动端效果

# 草稿
1、未绑定作品的草稿显示在灵感草稿箱http://localhost:3000/drafts，绑定作品的草稿显示在对应作品的管理页面，处于未发布状态

# 章节
1、章节创建逻辑需要自动检查当前已有的章节序号是什么，然后创建时标题自动累加
2、章节列表不能按照更新时间来排序，需要按照章节顺序来排序，可以正序倒序排列
3、作品管理里的章节页面需要可以提取细纲，提取人物角色，提取设定等。在桌面端考虑的是每个章节可以有个点击向右展开的小箭头，执行逻辑如下：点击弹出对话框可以选择操作大纲还是角色还是设定，选择大纲后，可以继续选择执行大纲提取，大纲编辑，大纲删除等等选项，选择具体操作如大纲提取，可以把当前章节的前文内容以及大纲提取的提示词注入到ai写作助手中，ai助手完成大纲提取后，返回大纲提取结果，其余的角色提取等功能类似，移动端的UI布局需要更巧妙友好的设计。
4、UI设计上需要考虑章节和大纲的对照联系交互体验。

# 角色
角色卡片列表，具体角色有角色关系网，角色经历时间线等功能


# 设定
设定卡片列表，具体设定有设定与设定的关联关系，设定与角色的关联关系（比如火眼金睛这是一个设定，类别是技能，但是它又是与孙悟空独一无二的技能，所以实际上它也需要关联角色孙悟空），世界地图等功能

---

# 当前实现进展对齐（以本分支代码为准）

## 已落地

1、工作台壳层组件已具备，可复用统一导航与模块容器：`frontend/src/features/works/components/WorkWorkspaceLayout.tsx`

2、作品总览页 `/works/[id]` 已实现“上半区作品信息 + 下半区内容管理标签”结构：`frontend/src/app/(main)/works/[id]/page.tsx`

3、草稿边界闭环已打通

- 全局 `/drafts` 固定为未关联作品的灵感草稿箱（workId=0）：`frontend/src/app/(main)/drafts/page.tsx`
- 作品草稿统一收敛到 `/works/[id]/drafts`（列表/新建/编辑/发布）：
  - `frontend/src/app/(main)/works/[id]/drafts/page.tsx`
  - `frontend/src/app/(main)/works/[id]/drafts/new/page.tsx`
  - `frontend/src/app/(main)/works/[id]/drafts/[draftId]/edit/page.tsx`
- 草稿卡片/列表支持 edit href 注入，避免作品草稿误跳全局编辑页：
  - `frontend/src/features/drafts/components/DraftCard.tsx`
  - `frontend/src/features/drafts/components/DraftList.tsx`

4、章节/角色页已接入工作台壳层（结构统一）

- 章节：`frontend/src/app/(main)/works/[id]/chapters/page.tsx`
- 角色：`frontend/src/app/(main)/works/[id]/characters/page.tsx`

## 仍待落地（与设计要求对齐）

1、章节排序规则（按章节顺序 + 正序/倒序切换）仍未实现，当前仅有 UI 占位提示。

2、新建章节默认标题递增（自动识别最大章节序号）仍未实现。

3、章节扩展操作与 AI 提取链路（大纲/角色/设定：提取/编辑/删除 + 提取中/完成/失败状态）仍需统一承载。

4、大纲与设定（world）页面当前仍为 mock 原型页，尚未统一接入工作台壳层：

- `frontend/src/app/(main)/works/[id]/outline/page.tsx`
- `frontend/src/app/(main)/works/[id]/world/page.tsx`
